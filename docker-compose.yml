version: '3'
services:
  mongodb:
    image: mongo:3.4
    volumes:
      - /tmp/mongo:/data
  talkbot:
    build: .
    command: /bin/sh -c "python setup.py install && talk_bot run -c /srv/config.json"
    volumes:
      - .:/srv
    links:
      - mongodb
    ports:
      - "443:443"
